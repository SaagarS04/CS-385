#include <cmath>
#include <iomanip>
#include <iostream>
#include <sstream>
#include <vector>

using namespace std;

int main(){
    int upper_limit = 50;
    vector <bool*> * is_prime;
    is_prime = new vector<bool*>(upper_limit);
    cout << is_prime << endl;
}